# Mana - 智能账单分析平台

自动分析支付宝、微信、银行卡账单，智能分类统计，异常检测提醒。

## 技术栈

- **前端框架**: React Router v7 (原 Remix)
- **运行时**: Cloudflare Workers & Pages
- **数据库**: Cloudflare D1 (SQLite)
- **存储**: Cloudflare R2
- **样式**: Tailwind CSS v4

## 功能特性

- 📊 支持多种账单格式（支付宝、微信、CSV）
- 🏷️ 智能分类消费记录
- 💰 预算管理和超支提醒
- 📈 消费趋势分析
- 🔔 异常支出检测

## 快速开始

### 安装依赖

```bash
npm install
```

### 本地开发

```bash
npm run dev
```

访问 http://localhost:3000

### 初始化数据库

```bash
# 创建 D1 数据库
wrangler d1 create mana-db

# 更新 wrangler.toml 中的 database_id

# 执行数据库迁移
wrangler d1 execute mana-db --file=app/lib/db/schema.sql
```

### 部署到 Cloudflare

```bash
npm run deploy
```

## 项目结构

```
mana/
├── app/
│   ├── routes/          # 页面路由
│   ├── components/      # React 组件
│   ├── lib/
│   │   ├── db/         # 数据库 schema
│   │   ├── parsers/    # 账单解析器
│   │   └── analyzers/  # 分析器（分类、异常检测）
├── public/             # 静态资源
└── wrangler.toml       # Cloudflare 配置
```

## 使用说明

### 上传账单

1. 从支付宝/微信导出 CSV 账单
2. 访问 `/bills/new` 上传文件
3. 系统自动解析并分类

### 查看统计

- 访问 `/analytics` 查看消费统计
- 访问 `/bills` 查看账单列表
- 访问 `/settings` 管理分类规则

## 开发指南

### 添加新的账单来源

在 `app/lib/parsers/` 中创建新的解析器，实现统一的接口：

```typescript
export async function parseNewFormat(file: File): Promise<ParsedBill[]>
```

### 自定义分类规则

在 `app/lib/db/schema.sql` 中修改 `categories` 表的初始数据。

## License

MIT
